# PORTING NOTES — upgrade-v2026.8.1-beta.2

Port of the 79-commit carried stack (tip of `upgrade-v2026.8.1-beta.1`,
7a6c5f29b6d5) onto the `v2026.8.1-beta.2` tag (8f382a202ff).

## Drops

- **7f5f2430c5dd** `test(auth): drop 6.11-era legacy auth.json inline-migration test` — no-op in the beta.2 dry run (already covered upstream); dropped.
- **cb188a7fc5ad** `fix(rebase): repair two ratchet failures the type lanes could not see` — no-op in the beta.2 dry run (already covered upstream); dropped.
- **5be1dc40be2** `fix(gateway): cap pending final delivery recovery` — semantically superseded upstream. Upstream #121969 (merged 2026-08-11, inside beta.2) deleted `src/agents/main-session-restart-dispatch.ts` + the flat `pendingFinalDelivery*` SessionEntry fields (retired at the storage layer by `src/config/sessions/store-entry-shape.ts`) and re-architected restart recovery around `src/agents/main-session-recovery/*` with bounded `mainRestartRecovery.chargedAttempts` (cap `MAX_RECOVERY_RETRIES`, exhausted → durable tombstone + notice) and pending final deliveries owned by the durable outbound delivery queue (`src/infra/outbound/delivery-queue-recovery.ts`: `maxRetries` budget → dead-letter to `failed/`). Replacement: upstream #121969 + delivery-queue retry budgeting.
- **d4d8ce7cb85** `refactor(agents): keep Anthropic long-context classifier internal to satisfy knip` — semantically superseded by the fe503cffa39 adaptation in this port: the classifier was re-anchored directly module-internal (non-exported, no barrel export, no external references) into `src/agents/failover/context-overflow.ts`, which already deleted the old `embedded-agent-helpers.isbillingerrormessage.test.ts` describe block. No separate commit needed.

## Adaptations (semantic ports, not textual merges)

- **d932d757295** `fix(gateway): ignore future pending final retry timestamps` — the original patch targeted the flat `pendingFinalDeliveryLastAttemptAt` backoff in the deleted `main-session-restart-recovery-store.ts`. Intent (future-dated pending final retry timestamps are ignored, not read as infinite backoff) re-anchored onto `isDeliveryRecoveryRetryEligible` in `src/infra/delivery-recovery.shared.ts`, the beta.2 home of pending final delivery retry timing; test added to `src/infra/delivery-recovery.shared.test.ts`.
- **fe503cffa39** `fix(agent): treat Anthropic long-context usage errors as context overflow` — upstream #121341 consolidated failover classification into `src/agents/failover/`; the classifier was re-anchored onto `isLikelyContextOverflowError` there. Because upstream #61608 lists "extra usage is required (for long context requests)" as a billing pattern, the check runs BEFORE the billing/rate-limit exclusions to win the classification race (otherwise the 429 would be classified billing and never compact+retry).
- **882bf436c4a** `docs+style: document compact+retry recovery path` — code portion already present in the ported classifier; only docs applied.
- **31f41ecfa5a** `fix(gateway): fail fast on missing local probe auth` — merged with upstream's new device-token scoping (sshTunnel/explicit/remote targets) and the renamed probe-auth surface resolver; `status.gather.ts` probes `probeUrl` (upstream moved it out of `gateway.probeUrl`).
- **b7d17d696dc** `feat(memory-lancedb): fallbackBaseUrl embedding failover` — upstream moved memory-lancedb `uiHints` into `openclaw.plugin.json`; the new `embedding.fallbackBaseUrl` hint lands there, config.ts keeps the functional plumbing only.
- **2db692785ad** `fix(agents): port #93952 auth-deadlock stack` — adapted to beta.2 `authProfilesLog` (constants export after collision burn #121768/#121893), `hasUsableOAuthCredential` from `./credential-state.js`, `asDateTimestampMs` from `@openclaw/normalization-core`, and upstream #123298 shared auth store seam (`resolveSharedAuthStorePath` for undefined ownerAgentDir). Functional content verified identical (delta vs beta.2 base contains only the in-lock deadline + abandoned write-back discard stack).
- **f30c33242f8** / **29b9a9bc24e** / **73697c24e23** / **c77b4bca1d0** / **7a6c5f29b6d** — rebase-hygiene commits; re-derived against the new base instead of ported verbatim (see entries below as they occur).

## Health runtime-config-drift re-derive (#54–#55)

- **65a1c4bde3d** `fix(gateway): surface runtime config drift in health, recompute on cache hits` and **95addccdcfe** `refactor(gateway): re-anchor runtime-config drift health onto relocated health modules` — functional drift code (RUNTIME_CONFIG_DRIFT_PATHS, buildRuntimeConfigHealthSummary, formatRuntimeConfigHealthLine, readRuntimeConfigDriftState, buildRuntimeConfigHealth in `src/commands/health.ts`; RuntimeConfigHealthSummary + HealthSummary.runtimeConfig in `src/gateway/health/types.ts`; config-reload-observed generation cache; cache-hit merge recompute in `src/gateway/server-methods/health.ts`; tests) re-anchored onto beta.2. The 7.1 commits carried an inline getHealthSnapshot + inline buildContextEngineHealthSummary/buildDeliveryQueueHealthSummary/readRuntimeHealthConfig into commands/health.ts; beta.2 builds the full snapshot in `src/gateway/health/collector.ts` (collectGatewayHealthSnapshot) and wires runtimeConfig only on the gateway cache-hit merge path (mergeCachedHealthRuntimeState in server-methods/health.ts), so the inline snapshot block was dropped and the duplicate formatEventLoopHealthLine removed. `resolveStorePath` (7.1 name) re-anchored to beta.2's `resolveSessionStorePathCore`. Dangling `import("../../commands/health.types.js")` type-only ref in server-methods.test.ts re-anchored to `../health/types.js` (that module never existed in the carried stack; the type lives in gateway/health/types.ts).
- **6b230d7bdd2** `test(health): split runtime-config drift tests into a sibling file` — applied; health.snapshot.test.ts dropped the two beta.2-obsolete imports (shared/number-coercion MAX_TIMER_TIMEOUT_MS already from normalization-core; createOpenClawTestState unused after the split).
- **8192dc30f76** `style(gateway): oxfmt server-methods drift-recompute tests` — types.ts hunk (removed a duplicate runtimeConfig field that did not exist in the re-derived layout) resolved to HEAD; test reformat applied.
- oauth-manager.ts (from the #93952 port, 2db692785ad): restored the trailing `oxlint-disable max-lines` grandfathering directive that the 7.1 final carries (accidentally dropped during adaptation); the max-lines ratchet (config/max-lines-baseline.txt) requires it.

## Gateway zombie-shutdown PR (#88908) re-anchors

- **44030898b** `fix(gateway): return 503 on /healthz during shutdown and tighten lock-recovery preflight` — beta.2 split `handleGatewayProbeRequest` out of `server-http.ts` into `server-http-probes.ts` (and plugin-auth into `server-http-plugin-auth.js`). The 503-on-shutdown live-probe logic + `noteShuttingDownProbeResponse`/`resetGatewayHealthzShuttingDownLogForTest` helpers now live in `server-http-probes.ts` (which owns the probe handler); `server-http.ts` threads `getShuttingDown` (opts + createGatewayHttpServer) and re-exports the test reset. `server-runtime-state.ts` gains the `getShuttingDown` param + passes it through. run.ts zombie_detected trace/warn applied unchanged. Item 4 of the message (probeGatewayHealthz 200-only) was already true at the 7.1 parent; no code change.
- **ae281327e04** `test(gateway): cover zombie-shutdown force-exit, healthz shutdown response, and lock-recovery preflight tightening` — the four new shutting-down 503 probe tests could not be added in-place to `server-http.probe.test.ts` (the beta.2 base file already sits at eff-998, the 1000 test max-lines budget), so they are split into a sibling `src/gateway/server-http.probe.shutdown.test.ts` (same harness imports; own afterEach resets). `server-close.test.ts` + `run.supervised-lock.test.ts` applied cleanly.
- **6f5aac85e74** `fix(gateway): address ClawSweeper review P1+P3 on zombie-shutdown PR` — strict-mode `?strict=1` narrowing re-anchored: `isStrictLiveProbeRequest` + gate live in `server-http-probes.ts` (beta.2's probe handler home); the four zombie 503 tests + two public-contract 200 tests live in the sibling `server-http.probe.shutdown.test.ts`; run.ts/server-close*.ts changes applied cleanly (strict path in probeGatewayHealthz, per-cycle zombie_detected dedupe, in-process restart watchdog skip).
- **665c2bc2608** `refactor(gateway): extract shutdown lifecycle state into lightweight module` — re-derived: `gateway-shutdown-state.ts` (state + per-cycle probe-log dedupe) created; `server-close.ts` re-exports; `server-start.ts` imports the lightweight module directly. The probe-log dedupe helpers (noteShuttingDownProbeResponse / reset) moved from `server-http-probes.ts` (my #59/#63 home) into `gateway-shutdown-state.ts`, and `server-http.ts` re-exports `resetGatewayHealthzShuttingDownLogForTest` as an alias of `resetShuttingDownProbeResponseLogForTest`. The new dedupe-cycle regression test was placed in the sibling `server-http.probe.shutdown.test.ts` (main probe.test.ts sits at the 1000-line test budget).
- #77 5d8345d081c "fix(rebase): align LanceDB embeddings and release evidence": memory-refresh.ts embed(agentId, text) re-derive APPLIED (required by beta.2 embeddings interface, 2 sites). The 525-line deletion of scripts/generate-dependency-release-evidence.mts NOT ported: on the 7.1 tip the script was absent and CI refs=0, but the beta.2 base (8f382a202ff) still ships the script AND references it from .github/workflows/ci.yml (x2) + openclaw-npm-release.yml (x1) — deleting it would break beta.2 CI. Kept the script (delete/modify conflict resolved as ours).

## Final picks (#78–#79)

- **06fe49e8c5f** `test(ui): stabilize beta reconnect E2E` — DROP (covered upstream): the beta.2 base (8f382a202ff) already ships the equivalent reconnect stabilization (waitForControlUiGatewayReconnecting helper, setOnline-based sidebar reconnect scenarios, deferNext parameter matching, scroll assertion). The pick produced only base-drift conflicts (10_000 vs controlUiE2eWaitTimeoutMs; includeLastMessage vs includeDerivedTitles — both beta.2 base conventions), zero net functional delta; cherry-pick aborted, no-op confirmed.
- **7a6c5f29b6d** `chore(lint): keep ported health command within ratchet` — RE-DERIVE: the original micro-trim (inline readBestEffortHealthConfig + import dedupe) is subsumed; instead the ported runtime-config-drift feature was split into src/commands/health-runtime-config.ts (re-exported from health.ts, all import paths unchanged) so commands/health.ts fits the 700 max-lines budget (~559 eff) without a grandfathering directive. Drift tests pass.
- Treadmill: pnpm run plugin-sdk:api:gen baseline (docs/.generated/plugin-sdk-api-baseline.sha256) committed separately after the build.

## Treadmill (plugin-sdk API)

- beta.2 ships `plugin-sdk:api:diff` (CI lane, informational: runs without --require-acknowledgement; no baseline file — the 7.1 `plugin-sdk:api:gen` + docs/.generated/plugin-sdk-api-baseline.sha256 mechanism does not exist in this base). Ran `pnpm run plugin-sdk:api:diff --base v2026.8.1-beta.2 --head HEAD`: the delta is exactly the carried stack's public surface (updateAuthProfileStoreWithLock gains agentLocalOnly; SnapshotSchema refresh) — no baseline commit possible or needed.

## Post-port build fixes + verification

- **9f7da26c71e** `fix(gateway): re-anchor parseStrictPositiveInteger import for beta.2` — tsdown-unified UNRESOLVED_IMPORT from the zombie-shutdown force-exit port (cab0e4f74d2): ../infra/parse-finite-number.js (7.1-only) → @openclaw/normalization-core/number-coercion (beta.2 home).
- Build: `pnpm run build` (scripts/build-all.mts) exit 0 after the fix; earlier 0465e9bbbcb (import-reorder cleanup after 39ba15cc6) was the only other structural fix.
- Verification gate (final): git status clean; 0 merge commits; `rg "pre-hook recovery cannot bypass" src/` → 0 hits (beta.1 fail-closed path absent; beta.2 defers on active plugin reply_dispatch hooks via findRestartRecoveryUnsafeChatAdmissionHook instead); `rg "UNRESUMABLE_SESSION_NOTICE" src/` → 0 hits (beta.1-only constant; beta.2's last-resort edge is TOMBSTONED_SESSION_NOTICE in main-session-restart-recovery-failure.ts, reached only on exhausted/tombstone, never via an active reply_dispatch hook); `rg -l main-session-restart-recovery-resume-policy dist/` → dist/main-session-restart-recovery-BbPe90KQ.js; main-session-recovery vitest suite → 12 files / 629 tests passed (52s).
- Accounting: 75 of 79 ordered picks landed as ported/re-derived commits; 4 documented above (5be1dc40be2 + d4d8ce7cb85 superseded; cb188a7fc5ad + 06fe49e8c5f already covered upstream). 7f5f2430c5dd was in the dry-run DROP list but ported (aac46341641): it deletes the test file that pick #1 (5e2b2b212b8) recreates, so the net carried-tip state (file absent) is reproduced — both commits land, matching the 7.1 tip.

## Post-port: missed upstream PRs (discovered 2026-08-20)

- **openclaw/openclaw#57280** `fix(telegram): skip reply media download when replied-to message is from bot` (head cfa1355c68b; superseding dupes #59242 63237c0ff / #66912 382f9da84) — NOT in the carried 79-stack (PR was barnacle-closed unmerged 2026-03-29 due to the >10 open-PR limit; never merged to main). Live regression observed 2026-08-20: one inbound Telegram image produced two identical `media://inbound/<id>` blocks (reply-chain hydration re-attached the current message's staged file). RE-DERIVED for beta.2 (handlers consolidated into bot-handlers.message-pipeline.ts / bot-message-context.session.ts; `isSelfAuthoredTelegramMessage` → `isTelegramMessageFromCurrentBot` from message-cache.ts): (a) reply-chain nodes authored by the current bot no longer hydrate/download media; (b) one staged file cannot be attached twice to a single prompt (dedupe by normalized `media://` path across current-message and reply-chain facts). New test: bot-message-context.reply-media-guard.test.ts (3 tests); bot.test.ts reply-ancestor cases updated to the new self-authored-skip behavior. Full telegram suite 3584/3584.
- **openclaw/openclaw#56166** `fix(agents): check transcript validity before marking subagent runs as orphaned` (head a14860c96ee8ada7f471859ef32235c3ea32f1fd) — barnacle-closed unmerged 2026-04-22; never merged to main. Symptom still live at beta.2 base: the sweeper's stale-unended branch (subagent-registry-sweeper.ts) error-finalizes a run as "subagent run lost active execution context" whenever the persisted session entry has no status — even when the child's JSONL transcript already holds a final assistant reply (gateway restart mid-flush). The PR's `hasCompletedTranscript` + in-place `reconcileOrphanedRun` mutation targeted the old single-file registry (subagent-registry-helpers.ts) that beta.2 split into the registry/ family. RE-DERIVED onto the beta.2 seam: (a) `resolveSubagentRecoveryCompletion` in subagent-session-reconciliation.ts combines the existing entry-status path with a new transcript-tail fallback (32KB tail scan, latest assistant reply with no pending tool_use/toolCall/function_call block, not after a newer user/system turn, stopReason error/aborted rejected, `sqlite:`-backed transcript targets fall through to the entry path only); (b) the sweeper's stale-unended branch swaps `loadSubagentSessionEntry` + `resolveCompletionFromSessionEntry` for the combined seam (net-zero effective lines; sweeper stays at the 700 max-lines budget). Same source label `sweeper-session-completion` as before (no new announce/cleanup path). New test: subagent-session-reconciliation.transcript-recovery.test.ts (9 cases, real SQLite store + JSONL transcript fixture). Sweeper-recovery / registry-helpers / orphan-recovery restart-integration suites green (96 tests).
- **openclaw/openclaw#56529** `fix(agent): overload failover backoff ceiling 1.5s -> 30s, configurable` (head 7b0b62a78aa76169f96d5b17a9d251180512cbf8) — amittell-authored, closed unmerged; never in main. Symptom verified live at branch HEAD: the pre-failover overload backoff was a flat, unconfigurable 0ms (no pacing before rotating to the next auth profile on `overloaded_error`), so sustained multi-profile overload burned the retry budget unpaced. RE-DERIVED: `resolveOverloadFailoverBackoffMs` (flat number) renamed to `resolveOverloadFailoverBackoffPolicy(cfg)` returning a `BackoffPolicy` (initial 250ms, factor 2, jitter 0.2, ceiling `maxMs`); ceiling defaults to 30s and is configurable via `agents.defaults.embeddedAgent.overloadBackoffMaxMs` (zod `.number().nonnegative()`, help + label entries), 0 restores legacy no-backoff. The failover-retry-controller tracks a per-run `overloadFailoverAttempts` counter and sleeps `computeBackoff(policy, attempts)` (from `src/infra/backoff.js`, re-exported from packages/retry) on each overload rotation. New test: failover-overload-backoff.test.ts (6 tests: policy defaults/override/invalid-override + escalation to ceiling, clamp at override ceiling, 0-disable). Scoped vitest 18/18 green; `grep -rn resolveOverloadFailoverBackoffMs src/` = 0 hits.
- **openclaw/openclaw#57137** `fix(auth): sync env-var-backed token credentials into the auth store` (head 17a97e50d5535f0033b8d4aa0f57bb8b9d0a54c0) — amittell-authored, barnacle-closed unmerged; never in main. Symptom verified live on branch HEAD: when an env var holding a token credential is changed after a session has persisted the old value, isolated/embedded runs keep failing every LLM request (stale persisted token) until the session is aborted, because the persisted profile shadows the fresh env var. RE-DERIVED for beta.2 (auth-profiles split into the src/agents/auth-profiles/ family): new `syncEnvBackedTokenCredentials(store, {env})` in external-cli-sync.ts — applies the profile-id → env var naming convention (`ANTHROPIC_ME_COM_TOKEN`), skips secret-ref-backed profiles (token value is a `${ENV}` template or a `tokenRef`) and non-token profiles, no-ops when the env var is unset or already matches the stored value, returns a fresh store clone (no in-place mutation). `syncPersistedExternalCliAuthProfiles` composes the sync before the CLI-candidate path so env-token sync applies even when no external CLI profiles exist. New test: auth-profiles.env-token-sync.test.ts (11 cases). Verification: 22/22 scoped vitest green. Commit 6f7fb578119 (recovered from a crashed dispatch — gpufarm model outage 2026-08-20 23:02-23:04 EDT killed the re-derive job mid-test; source was complete, one test callback missing `async`).
- **openclaw/openclaw#56532** `memory-lancedb: add configurable timeout/retry for embedding calls` (head de58b85d0eb7ef1861fe46f887108f44540be939) — amittell-authored, closed unmerged; never in main. Symptom verified live at branch HEAD: the OpenAI-SDK embedding transport had no operator-tunable timeout/retry budget (SDK default 600000ms), so a hung or 5xx-storming embedding backend stalls every agent turn that triggers auto-recall. RE-DERIVED for the beta.2 layout (embedding client split into embeddings.ts on top of the carried-stack fallbackBaseUrl failover substrate; uiHints manifest-owned): (a) config.ts gains `embedding.timeoutMs` / `embedding.maxRetries` (allowed keys, clamp-to-undefined: out-of-bounds, non-finite, or non-integer values silently fall back to the SDK defaults rather than throwing — the beta.2 runtime parser is throw-on-bad-value for other fields, so the silent-fallback semantics live in dedicated resolvers); (b) embeddings.ts `OpenAiCompatibleEmbeddings` carries the configured bounds and `postVia` applies them only to requests without a per-call timeout budget — the per-call budgets (auto-recall 5s / tool recall 15s, `maxRetries: 0`) still win so SDK retries never double a bounded deadline; (c) openclaw.plugin.json uiHints + configSchema (timeoutMs number 1000–60000, maxRetries integer 0–5). CHANGELOG.md intentionally untouched (carried-stack convention: beta.2 changelog frozen). Tests: config.test.ts +6 (accept/boundary/oob-fallback/non-finite/manifest-schema-reject/manifest-owned-uiHints); index.test.ts +1 written with the isolation-safe inline `vi.resetModules()` + `vi.doMock("openai")` pattern (unconfigured keeps SDK defaults and the 15s/0 budget; configured bounds ride timeout-less auto-capture embeds while the recall budget wins). Verification: new tests green in isolation and in full-suite order. Scoped directory run = 7 failed / 189 passed — the 7 failures are PRE-EXISTING at branch HEAD (test-isolation pollution introduced by the carried stack: `withMockedOpenAiMemoryPlugin`-based tests share one hoisted module mock and a prior inline-mock test poisons them; pristine beta.2 base 8f382a202ff runs 141/141 green). The failing set is byte-identical with and without this change. Commit 9492b45e6be.
